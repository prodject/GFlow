2026-07-22

Этап 1: Главный экран

Сделано:
- Полностью заменен shell главного экрана в `MainActivity`.
- Добавлена новая верхняя статусная панель в темной glass-теме.
- Добавлен постоянный левый collapsed navigation rail для home-сценария.
- Главный экран перестроен в вертикальную композицию под `Design.txt`: top bar, hero area, status cards, bottom dock.
- Hero-блок переработан под схему из `Design.txt`: погодная карточка, центральная машина, быстрые зоны перехода.
- Добавлен новый блок статусных карточек: климат, готовность авто, DVR, ADAS, 360/Parking, профиль.
- Добавлен нижний быстрый dock: климат, 360, DVR, drive mode, голос, профиль.
- Обновлен `Ui.java`: добавлены темные цвета, glass-card стили и текстовые хелперы для нового дизайна.
- Главный экран переведен на live-state обновление: верхняя панель, hero-блок погоды и сводка автомобиля теперь читают реальные/кешированные данные вместо статичных строк.
- Для home добавлен `expanded drawer` поверх нового shell: быстрый переход по крупным разделам, статусы профиля/погоды/салона и scrim-анимация.
- Для hero, статусных карточек и нижнего dock добавлены нижние quick-action sheets, закрывающие сценарий контекстных действий на главном экране.

Осталось по `Design.txt`:
- Экран `Климат`.
- Экран `Автомобиль / Кузов`.
- Экран `DVR / Камеры`.
- Экран `ADAS / Вождение`.
- Экран `Парковка / APA`.
- Экран `HUD / Cluster / OneOS`.
- Экран `Автоматизация`.
- Экран `Профили`.
- Экран `Кнопки руля`.
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.
- Приведение навигации всего приложения к единому вертикальному шаблону.
- Замена оставшихся старых экранов с legacy-layout на новый дизайн-язык.

Проверка:
- Автосборка в текущей среде не выполнена: отсутствует `gradle`, и в репозитории нет `gradlew`.

2026-07-22

Этап 2: Экран Климат

Сделано:
- `showComfortClimate()` переведен со старого `commandRoot` на отдельный полноэкранный shell в новом стиле.
- Добавлена верхняя панель экрана `Климат` с новым стеклянным оформлением.
- Добавлена `Comfort Panel` с крупными быстрыми HVAC-плитками: HVAC, Auto, A/C, A/C Max, Eco, Sync, Split, °C/°F.
- Добавлен основной климатический layout: зона водителя, центральный airflow visual, зона пассажира.
- Добавлен новый fan slider в центральном блоке.
- Добавлены быстрые действия для seat heat, seat vent, wheel heat и defrost.
- Добавлены климатические пресеты быстрого доступа: тихий, комфорт, прогрев.
- Добавлена нижняя action-панель: Auto, A/C, Defrost, Smart, Расширенно.
- В `ClimateActivity` добавлен полноценный `Advanced HVAC` flow внутри нового дизайна: air quality, defrost/dry, zones/sync, pre/post climate, rapid cooling/warming и hardkey actions.
- В экран добавлены настоящие bottom sheets для quick HVAC actions, defrost, temperature presets, readback и dock-context действий.
- Добавлен live HVAC readback по основным и расширенным параметрам: core HVAC, температуры driver/passenger, seats/wheel, air quality, defrost/dry, advanced modes.
- Добавлены периодические live-state обновления top bar, hero-summary и HVAC summary внутри `ClimateActivity`.
- Все основные входы раздела `Климат` из `MainActivity` переведены на новый `ClimateActivity`; `Advanced` теперь открывается в новом экране, а не в legacy HVAC-layout.
- Внутрь `ClimateActivity` перенесены сохраненные пользовательские climate presets: отдельный экран списка, карточки применения/редактирования/удаления и встроенный preset editor.
- Внутрь `ClimateActivity` перенесен `Smart Climate`: отдельный mode-flow с настройками контроллера и ручным запуском шага алгоритма.
- `ClimateActivity` расширен до многоэкранного раздела `Comfort / Advanced / Presets / Preset Editor / Smart` без возврата в старый `MainActivity`-экран.
- Верхнеуровневые climate quick-actions на главном экране (`Климат`, `Умный климат`, `Пресеты климата`) теперь открывают только новый `ClimateActivity`.
- Legacy climate UI и climate preset/storage helper-ветки удалены из `MainActivity`; старый климатический экран больше не используется как fallback.

Осталось по `Design.txt`:
- Экран `Автомобиль / Кузов`.
- Экран `DVR / Камеры`.
- Экран `ADAS / Вождение`.
- Экран `Парковка / APA`.
- Экран `HUD / Cluster / OneOS`.
- Экран `Автоматизация`.
- Экран `Профили`.
- Экран `Кнопки руля`.
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.
- Реализация expanded drawer, context sheets и анимаций уровня `Design.txt`.

2026-07-22

Структурный рефакторинг после этапа 2

Сделано:
- Новый климатический экран вынесен из `MainActivity` в отдельный `ClimateActivity`.
- `MainActivity` теперь только открывает `ClimateActivity`, а не содержит его полноэкранный shell.
- `ClimateActivity` подключен в `AndroidManifest.xml`.
- В `MainActivity` удален крупный блок layout- и action-логики нового климатического экрана.

Результат:
- Структура стала чище: главный экран остается в `MainActivity`, отдельный большой экран вынесен в собственный класс.
- Дальнейшие экраны (`Автомобиль`, `ADAS`, `DVR`, `Парковка`) лучше выносить по той же схеме, а не накапливать в одном activity-файле.

2026-07-22

Этап 3: Экран Автомобиль

Сделано:
- Создан отдельный `VehicleActivity` для экрана `Автомобиль / Кузов`.
- `showVehicleMenu()` в `MainActivity` теперь только открывает `VehicleActivity`.
- `VehicleActivity` подключен в `AndroidManifest.xml`.
- Добавлен новый vehicle shell в стиле `Design.txt`: top bar, hero-panel, body controls, status cards, bottom dock.
- Внутри нового экрана добавлены быстрые действия для замков, окон, люка, шторки, зеркал, света и drive mode.
- Добавлен собственный визуальный блок автомобиля для body/access сценариев.
- `VehicleActivity` расширен до полноценного многоэкранного раздела: `Body`, `Seats`, `Mirrors`, `Lights`, `Drive`.
- Внутри нового экрана добавлены вложенные flow для сидений и memory, зеркал и roof, света и внешних сигналов, drive modes / steering / custom keys.
- Добавлены `bottom sheets` и context actions для body/seat/mirror/light/drive сценариев.
- Добавлен live readback по кузову, свету, drive/seats и roof/mirror прямо в новом экране.
- Лишний legacy-router `showCar()` удален из `MainActivity`.
- Пользовательский экран `Автомобиль / Кузов` полностью живет в `VehicleActivity`; `MainActivity` больше не содержит отдельного vehicle-screen/fallback для этого раздела.
- Raw vehicle-команды и диагностические блоки, которые еще остаются в `MainActivity`, относятся только к experimental/system-сценариям и не являются старым экраном `Автомобиль`.

Осталось по `Design.txt`:
- Экран `DVR / Камеры`.
- Экран `ADAS / Вождение`.
- Экран `Парковка / APA`.
- Экран `HUD / Cluster / OneOS`.
- Экран `Автоматизация`.
- Экран `Профили`.
- Экран `Кнопки руля`.
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 4: Экран ADAS / Вождение

Сделано:
- Создан отдельный `AdasActivity` для экрана `ADAS / Вождение`.
- `showAdasMenu()` в `MainActivity` теперь только открывает `AdasActivity`.
- `AdasActivity` подключен в `AndroidManifest.xml`.
- Добавлен новый ADAS shell в стиле `Design.txt`: top bar, hero-panel, safety controls, ACC/ICC panel, status cards, bottom dock.
- Внутри нового экрана добавлены быстрые действия для AEB, FCW, LKA, LDW, RCW, ELKA, ACC, ICC, TSR и PDC.
- Добавлен отдельный визуальный блок ассистентов движения.
- Экран расширен до полного покрытия раздела: внутри `AdasActivity` добавлены новые панели `PDC / Parking Assist`, `Experimental ADAS` и `Developer Diagnostics`.
- Парковочные ассистенты больше не считаются хвостом legacy ADAS: переход в parking-сценарии идет в отдельный `ParkingActivity`.
- Experimental ADAS-команды, preset `Highway assist ready`, support/readback-карточки и export diagnostics перенесены в новый UI `AdasActivity`.
- Legacy `showAdas()` и связанные raw/helper-ветки удалены из `MainActivity`.

Осталось по `Design.txt`:
- Экран `DVR / Камеры`.
- Экран `Парковка / APA`.
- Экран `HUD / Cluster / OneOS`.
- Экран `Автоматизация`.
- Экран `Профили`.
- Экран `Кнопки руля`.
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 5: Экран DVR / Камеры

Сделано:
- Создан отдельный `CameraActivity` для экрана `DVR / Камеры`.
- Основные входы из `MainActivity` переведены с `DvrActivity` на `CameraActivity`.
- `CameraActivity` подключен в `AndroidManifest.xml`.
- Добавлен новый camera shell в стиле `Design.txt`: top bar, hero-panel, recording controls, status cards, bottom dock.
- Внутри нового экрана добавлены быстрые действия для старта/остановки записи, EVS rear, EVS 360 и EVS DVR.
- В экран встроены live summary по архиву и доступным Camera2 / EVS источникам.
- В `CameraActivity` перенесены детальные настройки архива: cameras, segment, limit, storage, storage path и quality.
- В `CameraActivity` перенесены EVS / DVR diagnostics: availability, camera online, capture, current mode и SD status.
- Из нового экрана удалены переходы в legacy `DvrActivity`; fallback-маршрут больше не используется в основном пользовательском flow.
- Из `MainActivity` удален старый `showDvr()`-экран, который больше не участвовал в пользовательской навигации.

Осталось по `Design.txt`:
- Экран `Парковка / APA`.
- Экран `HUD / Cluster / OneOS`.
- Экран `Автоматизация`.
- Экран `Профили`.
- Экран `Кнопки руля`.
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 6: Экран Парковка / APA

Сделано:
- Создан отдельный `ParkingActivity` для экрана `Парковка / APA`.
- Основные входы из `MainActivity` переведены на `ParkingActivity`.
- `ParkingActivity` подключен в `AndroidManifest.xml`.
- Добавлен новый parking shell в стиле `Design.txt`: top bar, hero-panel, parking controls, status cards, bottom dock.
- Внутри нового экрана добавлены быстрые действия для Auto Park, 360, PDC, RCTA и выбора parking mode.
- Добавлен отдельный визуальный parking-блок.
- В `ParkingActivity` добавлен новый advanced parking flow для `APA / RPA`: raw park modes, button actions, remote parking и HAL diagnostics.
- В `ParkingActivity` добавлен новый advanced parking flow для `PAS / AVM`: PAC/AVM camera state, radar work modes, overlays, SAP и RCTA.
- Legacy `showParkingApa()` и `showPasAvm()` удалены из `MainActivity`; parking experimental/raw сценарии больше не живут в старом экране.

Осталось по `Design.txt`:
- Экран `HUD / Cluster / OneOS`.
- Экран `Автоматизация`.
- Экран `Профили`.
- Экран `Кнопки руля`.
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 7: Экран HUD / Cluster / OneOS

Сделано:
- Создан отдельный `HudActivity` для экрана `HUD / Cluster / OneOS`.
- `showHudMenu()` в `MainActivity` теперь только открывает `HudActivity`.
- `HudActivity` подключен в `AndroidManifest.xml`.
- Добавлен новый HUD shell в стиле `Design.txt`: top bar, hero-panel, projection controls, status cards, bottom dock.
- Внутри нового экрана добавлены быстрые действия для HUD, navigation/media/safety display, DIM night, presentation, tab switching и сервисов.
- Добавлен отдельный визуальный projection-блок.
- В `HudActivity` добавлен новый advanced flow для `HUD / DIM Bridge`: raw HUD commands, DIM menu, navigation modes, climate readback и media mute state.
- В `HudActivity` добавлен новый advanced flow для `AudioExt / Services`: media bridge, VR, PDC volume, anti-shake, loudness, spectrum preset и service actions.
- Legacy `showHud()` и связанные `HudDim` / `AudioExt` helper-ветки удалены из `MainActivity`.

Осталось по `Design.txt`:
- Экран `Автоматизация`.
- Экран `Профили`.
- Экран `Кнопки руля`.
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 8: Экран Автоматизация

Сделано:
- Создан отдельный `AutomationActivity` для экрана `Автоматизация`.
- `showAutomation()` в `MainActivity` теперь только открывает `AutomationActivity`.
- `AutomationActivity` подключен в `AndroidManifest.xml`.
- Добавлен новый automation shell в стиле `Design.txt`: top bar, hero-panel, overview grid, templates/actions, status cards, bottom dock.
- Внутри `AutomationActivity` реализованы полноценные новые экраны и сценарии для `preset`, `scenario`, `trigger`, `execution log` и `low-speed camera rule`.
- Для `preset/scenario/trigger` добавлены отдельные новые list/editor flow внутри нового дизайна: создание, редактирование, запуск/проверка и удаление.
- В экран встроены summary по preset/scenario/trigger, runtime preview и отдельный execution log с очисткой.
- Добавлены install-flow для готовых automation-шаблонов: climate, welcome/leave, parking guard, rain, night mode, navigation.
- Automation persistence и install-логика вынесены из `MainActivity` в отдельный `AutomationStore`.
- Legacy automation-ветки и raw-редакторы удалены из `MainActivity`.

Осталось по `Design.txt`:
- Экран `Профили`.
- Экран `Кнопки руля`.
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 15: Экран Профили

Сделано:
- Создан отдельный `ProfileActivity` для экрана `Профили пользователей`.
- Все основные входы из `MainActivity` переведены на `ProfileActivity`: dashboard widget, bottom dock, drawer и rail.
- `ProfileActivity` подключен в `AndroidManifest.xml`.
- Добавлен новый profiles shell в стиле `Design.txt`: top bar, hero-panel, overview grid, списки driver/passenger, bottom dock.
- Внутри нового экрана добавлена активная карточка профиля: имя, тип, identity metadata, avatar и время последнего применения.
- Реализованы отдельные list/editor flow для driver и passenger профилей: создание, выбор, применение, редактирование и удаление.
- Добавлен create/capture flow `Сохранить текущее`: имя, тип, avatar, source identity и выбор, какие настройки сохранить в профиль.
- `UserProfileEngine` расширен metadata-полями `avatar` и `last applied timestamp`, а также helper-логикой для capture текущих настроек в новое тело профиля.
- Переходы в `Профили` из `VehicleActivity` переведены на новый `ProfileActivity`.
- Legacy `showUserProfiles()` и связанные profile editor/helper-ветки удалены из `MainActivity`.

Осталось по `Design.txt`:
- Экран `Кнопки руля`.
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 16: Экран Кнопки руля

Сделано:
- Создан отдельный `SteeringActivity` для экрана `Кнопки руля`.
- Основные входы из `MainActivity` переведены на `SteeringActivity`: drawer и navigation rail.
- `SteeringActivity` подключен в `AndroidManifest.xml`.
- Добавлен новый steering shell в стиле `Design.txt`: top bar, hero-panel, overview grid, visual steering block, список назначений и bottom dock.
- Внутри нового экрана добавлены отдельные flow для списка назначений и editor binding: `press/double/triple/hold`, modifier, conditions, behavior и target types.
- Добавлен отдельный visual steering-блок с зонами `MODE / VOICE / MENU` и отображением последнего события.
- В новый экран перенесены быстрые сценарии `Hold`, `Double`, `Triple`, `Press` и установка примеров назначений из `Design.txt`.
- Добавлены встроенные примеры `hold 360`, `double cooling`, `voice hold`, `mute media`, `eco comfort toggle`, `stationary trunk`.
- Legacy `showSteeringButtons()`, `showSteeringButtonEditor()`, `installSteeringButtonExamples()` и steering overview helper удалены из `MainActivity`.

Осталось по `Design.txt`:
- Экран `Голос`.
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 17: Экран Голос

Сделано:
- `VoiceActivity` полностью переведен на новый shell в стиле `Design.txt`: top bar, hero-panel, overview grid, command panel, alias panel, log panel и bottom dock.
- В верхнюю часть экрана добавлены статусы `Foreground voice service` и `Vosk status`.
- В центральный блок добавлены большая voice hero-карточка, текст распознавания и основные действия `слушать Vosk`, `стоп`, `выполнить`, `ручной ввод`.
- Сохранена и встроена существующая parser-логика встроенных голосовых команд для climate/body/HUD/drive/DVR сценариев.
- Alias-команды вынесены в отдельный новый flow: добавление, редактирование, удаление, тест alias и установка примеров.
- Добавлен новый log/fallback flow: неизвестные команды уходят в `broadcast fallback`, логируются и могут быть сразу сохранены как alias.
- Экран теперь показывает source/event контекст, если голос вызван со steering или другого внешнего trigger.

Осталось по `Design.txt`:
- Экран `Рабочий стол`.
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 18: Экран Рабочий стол

Сделано:
- `DesktopActivity` полностью переведен на новый shell в стиле `Design.txt`: top bar, hero-panel, overview grid, weather widget, dock apps, OneOS Dock panel и bottom dock.
- В верхнюю часть добавлены desktop-статусы: количество pinned apps, размер app library и короткая weather summary.
- В hero-блок добавлены время, дата, активный профиль, preview dock apps и быстрые действия `Dock apps`, `OneOS Dock`, `Тема`, `Погода`.
- Пины рабочего стола вынесены в отдельный новый flow: список dock apps, изменение порядка, удаление, очистка и сохранение порядка.
- App library перенесена в новый flow: запуск приложения, pin в dock и переход к удалению пакета.
- Погодный виджет встроен в новый экран с сохранением cache/update логики и переходом на `WeatherActivity`.
- OneOS Dock вынесен в отдельную новую панель со status/hand-over/show-hide/HVAC/media/defrost/volume/custom icon командами.
- Данные `desktopPins` продолжают сохраняться в том же shared preferences формате, совместимом с `UserProfileEngine`.
- Legacy desktop-экрана внутри `MainActivity` не оставалось: сохранен только navigation entry `showLauncher()` с переходом в `DesktopActivity`.

Осталось по `Design.txt`:
- Экран `Браузер / Погода`.
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 19: Экран Браузер / Погода

Сделано:
- `WeatherActivity` полностью переведен на новый shell в стиле `Design.txt`: top bar, hero-panel, overview grid, weather controls, browser panel, forecast/bookmarks panel и bottom dock.
- В weather-часть добавлены все пункты из `Design.txt`: текущая погода, 3D-подобная иконка, температура, ветер, описание по `Open-Meteo code`, latitude, longitude и обновление погоды.
- Реализованы действия `обновить`, `изменить координаты`, `использовать текущие координаты` и `открыть прогноз`.
- Browser-flow вынесен в отдельный новый блок: address/search bar, `открыть сайт`, `поиск`, `reload`, `bookmark`.
- Список закладок перенесен в отдельный режим с сохранением, быстрым открытием и удалением по долгому нажатию.
- Weather now uses `Open-Meteo current + daily forecast` и сохраняет короткий forecast summary локально.
- Сохранены переходы из `DesktopActivity` и `MainActivity` на новый `WeatherActivity`.
- Legacy browser/weather-экрана внутри `MainActivity` не оставалось: сохранен только navigation entry `showWeb()` с переходом в `WeatherActivity`.

Осталось по `Design.txt`:
- Экран `Файлы`.
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 20: Экран Файлы

Сделано:
- `FileManagerActivity` полностью переведен на новый shell в стиле `Design.txt`: top bar, hero-panel, overview grid, storage card, actions panel, file list и bottom dock.
- В верхнюю часть добавлены путь, storage status, USB lookup и move queue статус.
- Реализована отдельная `Storage Card` с показом `свободно / занято / всего`, internal storage path и USB candidates.
- Список файлов вынесен в новый card-flow с действиями `открыть`, `поделиться`, `удалить`, `выбрать для перемещения`, `открыть как text`, `открыть как media`.
- Сохранены и встроены действия `создать папку`, `переход вверх`, `USB lookup` и `переместить сюда`.
- Cross-volume move fallback теперь явно отражен в UI: если `renameTo()` не сработал, выполняется `copy/delete`, показывается progress/status и при ошибке delete исходник не удаляется.
- Переходы на `TextViewerActivity` и `MediaViewerActivity` сохранены через новый file action flow.
- Legacy file-manager shell удален; экран больше не использует старую утилитарную разметку.

Осталось по `Design.txt`:
- Экран `Медиа`.
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 21: Экран Медиа

Сделано:
- `MediaViewerActivity` полностью переведен на новый shell в стиле `Design.txt`: top bar, hero-panel, overview grid, viewer panel, info panel и bottom dock.
- Для изображений добавлен fullscreen viewer с pinch zoom, swipe `next/prev`, `share`, `delete` и `info`.
- Для видео добавлен `VideoView + MediaController` с playback/seek и fullscreen toggle внутри нового viewer flow.
- Реализована gallery-навигация по соседним media-файлам из той же папки, чтобы `prev/next` работали при открытии из `FileManagerActivity`.
- Добавлены отдельные быстрые действия `Prev`, `Next`, `Share`, `Delete`, `Fullscreen`, `Info`.
- В info-панель и info-sheet вынесены путь, имя, размер, тип и gallery position.
- Legacy media-viewer shell удален; экран больше не использует старую минимальную разметку.

Осталось по `Design.txt`:
- Экран `Текст`.
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 22: Экран Текст

Сделано:
- `TextViewerActivity` полностью переведен на новый shell в стиле `Design.txt`: top bar, hero-panel, overview grid, search panel, viewer panel и bottom dock.
- Поддержка `.txt`, `.log`, `.json`, `.xml` и других текстовых файлов сохранена через новый viewer flow.
- Реализованы функции `поиск по тексту`, `копировать`, `share`, `monospace mode` и `wrap lines on/off`.
- Добавлен отдельный search flow с input, поиском первого совпадения и highlight найденного участка текста.
- В viewer-панель добавлены переключаемые режимы `monospace/default` и `wrap on/off`, а также text-selectable rendering.
- Вынесены file metadata: имя, путь, размер, количество строк и тип файла.
- Legacy text-viewer shell удален; экран больше не использует старую минимальную разметку.

Осталось по `Design.txt`:
- Экран `ADB / Система`.
- Экран `Настройки`.

2026-07-22

Этап 23: Экран ADB / Система

Сделано:
- `AdbShellActivity` полностью переведен на новый shell в стиле `Design.txt`: top bar, hero-panel, overview grid, permissions card, shell panel, ADB/DPI panel, autozoom panel, output panel и bottom dock.
- В permissions card добавлены `Accessibility`, `WRITE_SETTINGS`, `All files access`, `app card` и `grants check`.
- Shell-flow вынесен в отдельный новый блок: ввод команды, `выполнить`, показать результат, `скопировать`, `очистить`.
- Добавлен отдельный `ADB / DPI` блок с `ADB toggle attempt`, `DPI 440` и проверкой `adb_enabled`.
- Autozoom-flow перенесен в новый экран: `packages`, `scale`, `save autozoom`, `autozoom on/off`.
- Системный output теперь собирается в единый новый result panel и hero status.
- Legacy заглушка `showSystem()` удалена из `MainActivity`: navigation теперь открывает `AdbShellActivity` напрямую.

Осталось по `Design.txt`:
- Экран `Настройки`.

2026-07-22

Этап 24: Экран Настройки

Сделано:
- Создан отдельный `SettingsActivity` для экрана `Настройки`.
- `SettingsActivity` полностью переведен на новый shell в стиле `Design.txt`: top bar, hero-panel, overview grid, general settings panel, updates panel, diagnostics panel и bottom dock.
- В general settings вынесены `Experimental features toggle`, `Developer diagnostics toggle`, `Theme`, `Accent color` и `Start screen`.
- В updates flow перенесены `проверить GitHub releases`, поиск APK asset, `скачать APK` и `установить скачанный APK`.
- В diagnostics flow перенесены `автодиагностика`, генерация `gflow-diagnostics.txt` и отправка лога.
- Все основные входы из `MainActivity` на `Настройки` переведены на новый `SettingsActivity`.
- Legacy встроенный settings/update/diagnostics shell удален из `MainActivity`.
- `SettingsActivity` подключен в `AndroidManifest.xml`.

Осталось по `Design.txt`:
- Основные экраны по `Design.txt` закрыты полностью.
